# Plan: Update AGENTS.md for obsidian-lynx

## Goal
Compact, high-signal instruction file at `/Users/mrstruijk/Obsidian/digital_garden/.obsidian/plugins/obsidian-lynx/AGENTS.md` to help future OpenCode sessions ramp up and avoid repo-specific mistakes.

## Current assessment
- AGENTS.md already exists and is mostly accurate, but:
  - It says the plugin id is `lynx` (`manifest.json:2`) — actual `manifest.json` has `id: "obsidian-lynx"` at line 2.
  - It mentions `minAppVersion` is `1.7.2` (`manifest.json:5`) — actual `manifest.json:5` is `1.7.2`, but `versions.json:2` maps `1.0.0` to `1.13.0`, which conflicts and needs a note.
  - It says "three settings render and persist" in manual testing, but `settings.ts` now exposes **six** settings (default sort, show unresolved, open on startup, incoming/outgoing color, incoming/outgoing icon).
  - It omits `note-picker-modal.ts` and the `pick-linked-note` / `toggle-links-view` / `go-back` / `go-forward` commands added since the file was written.
  - It lists `npm run build` as the exact CI step, which is still true (`.github/workflows/lint.yml:25-27`).
  - It claims root `.mjs`/`.mts` files are not typechecked — true (`tsconfig.json:18` only includes `src/**/*.ts`).
  - It notes `eslint-plugin-obsidianmd/detach-leaves` rejects `detachLeavesOfType`, but the current code already uses `leaf.detach()` in `toggleLinksView` (`src/main.ts:133`) and the plugin no longer calls `detachLeavesOfType` anywhere, so the warning is still relevant but should be sharpened.
  - The `minAppVersion` rationale mentions `Workspace.revealLeaf` and `getSettingDefinitions()` — should verify `revealLeaf` min version is indeed `1.7.2` via Obsidian docs or keep as inherited context.

## Verified facts to preserve / update
- Project: Obsidian community plugin, TypeScript in `src/`, esbuild bundle to `main.js`.
- Entry: `src/main.ts` → `LynxPlugin`.
- Files:
  - `src/main.ts` lifecycle + 5 commands.
  - `src/links-view.ts` right-sidebar view + history + sort.
  - `src/links-collector.ts` incoming/outgoing link aggregation.
  - `src/settings.ts` 6 settings.
  - `src/note-picker-modal.ts` quick-open modal.
- Commands (sentence case): Open links view, Toggle links view, Go back, Go forward, Pick linked note.
- Build:
  - `npm run dev` → watch with inline sourcemap.
  - `npm run build` → `tsc -noEmit -skipLibCheck && esbuild production`.
  - `npm run lint` → ESLint flat config using `eslint-plugin-obsidianmd` recommended.
- TypeScript only typechecks `src/**/*.ts`.
- `manifest.json` id is `obsidian-lynx`; do not change after release.
- `minAppVersion` is `1.7.2` in `manifest.json`, but `versions.json` says `1.13.0` — user confirmed to correct `versions.json` to `1.7.2` and keep them in sync.
- `isDesktopOnly: false` → no Node/Electron APIs.
- esbuild external list: obsidian, electron, all codemirror/lezer packages, node builtins.
- Undocumented `MetadataCache.getBacklinksForFile` used in `src/links-collector.ts:72`.
- Do not use `detachLeavesOfType` in `onunload`.
- Release: `npm version patch|minor|major` (`.npmrc` suppresses `v` prefix); tag must equal manifest version; workflow creates draft release with `main.js`, `manifest.json`, `styles.css`.
- CI: Node 20/22/24, `npm ci`, `npm run build`, `npm run lint`.
- No tests; verify via build + lint + manual install.

## Open question before editing
- `versions.json` currently says `"1.0.0": "1.13.0"` while `manifest.json` says `minAppVersion: "1.7.2"`. Should `versions.json` be corrected to `"1.0.0": "1.7.2"` as part of this task, or left alone? This affects how we document the `minAppVersion` floor.

## Proposed AGENTS.md structure
1. Project type (Obsidian plugin, entry, output).
2. Build & check (commands, typecheck scope).
3. Code style (EditorConfig, strict flags, module split rule).
4. Architecture (one bullet per source module with current responsibilities and commands list).
5. Obsidian constraints (id, minAppVersion discrepancy note, external list, undocumented API, no detachLeavesOfType, sentence case).
6. Version & release (bump command, tag rule, workflow assets).
7. CI (matrix, steps).
8. Manual testing (copy artifacts, command palette checks, settings count).
9. LSP / References.

## Verification plan
- After editing, run `npm run build` and `npm run lint` to confirm no accidental breakage.
- Confirm AGENTS.md references line numbers that align with current file contents.
- Optionally run `git diff AGENTS.md` to review.
