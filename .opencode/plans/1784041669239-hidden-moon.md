# Finish sort order: Type (incoming first)

## Goal
Wire up the three existing type-based sort orders (`type-bi`, `type-asc`, `type-desc`) so their labels match their actual ordering.

## Current state
- `src/settings.ts` already exposes the dropdown options.
- `src/links-view.ts` declares `type-bi`, `type-asc`, `type-desc` in the view dropdown, but `sortItems()` uses a single rank regardless of direction.
- Existing rank: `{ bidirectional: 0, incoming: 1, outgoing: 2 }`.

## Required behavior (option A)
- `type-bi`: bidirectional → incoming → outgoing, then name ascending.
- `type-asc`: incoming → bidirectional → outgoing, then name ascending.
- `type-desc`: outgoing → bidirectional → incoming, then name ascending.
- Other sorts (`modified-*`, `name-*`) keep current behavior.

## Files to modify
- `src/links-view.ts`: update `sortItems()` to pick the correct type rank per direction.

## Verification
1. Run `npm run build` — should pass `tsc -noEmit -skipLibCheck` and esbuild.
2. Run `npm run lint` — no ESLint errors.
3. Manual test in Obsidian:
   - Open a note with incoming, outgoing, and bidirectional links.
   - Cycle through the three type sorts in the Lynx panel and confirm order matches the labels.
   - Confirm name-asc and modified-desc still sort correctly.
